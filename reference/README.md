# `reference/`

Read-only snapshots of files that live on `mnr-race` and are worth having in git, but that
nothing here deploys.

## `acevo-config/`

The league-tuned AC EVO server configs — `cars.json`, `events_practice.json`,
`events_race_weekend.json` — copied from `C:\Users\MNR\Desktop\mnr\ACEvoManager\` on
2026-08-18. They differ from Steam stock; CLAUDE.md's long-standing warning is not to
overwrite them from a stock copy.

Taken as insurance while reclaiming disk: the old `ACEvo_Latest\` install (which held a
byte-identical copy) was deleted, leaving `ACEvoManager\` as the only copy on the box.
